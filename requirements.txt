# ── Desktop app ─────────────────────────────────────────────────────────────
PyQt5>=5.15.0
pygame>=2.1.0

# ── Phase 2: local DB + sync ─────────────────────────────────────────────────
requests>=2.31.0          # HTTP client for sync engine

# ── Phase 2: web API server (run separately on a VPS or locally) ─────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0    # JWT tokens
passlib[bcrypt]>=1.7.4              # Password hashing
python-multipart>=0.0.9             # File upload support (FastAPI)
aiofiles>=23.0.0                    # Async file download support

# ── Phase 3: web dashboard ────────────────────────────────────────────────────
jinja2>=3.1.0                       # HTML template rendering

# ── Phase 4: advanced audio processing & commercial engine ────────────────────
pydub>=0.25.1                       # silence detection + audio normalisation
numpy>=1.24.0                       # (pydub dependency for sample math)

# ── Phase 5: live microphone ingest & auto-ducking ────────────────────────────
sounddevice>=0.4.6                  # low-latency mic capture + passthrough (needs PortAudio)
